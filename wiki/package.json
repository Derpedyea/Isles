{
  "name": "wiki",
  "type": "module",
  "version": "0.0.1",
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf-preview": "bun run build && wrangler dev",
    "cf-check": "bun run build && wrangler deploy --dry-run",
    "cf-deploy": "wrangler deploy",
    "cf-preview-deploy": "wrangler versions upload",
    "astro": "astro",
    "deploy": "bun run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^6.4.4",
    "wrangler": "^4.98.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
